

export interface dataChunkingType {
    name: String,
    description: String,
}
export interface getDataChunkType {
    base64Files: any,
    selectedOption: String,
    valueChunkOverlap: Number,
    valueChunkSize: Number

}